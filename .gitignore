# Go
/bin/
*.test
*.out
coverage.*

# the embedded FFmpeg WASM build artifact (built by the wasm pipeline, not committed
# raw until the build is reproducible + licensing is settled — see spec §7/§9)
/internal/wasm/ffmpeg.wasm
/internal/wasm/*.wasm

# docs site build + tooling caches
/site/
/dist/
/.cache/

# locally-saved CI logs
*.log

# editor / OS
.DS_Store
*.swp
.idea/
.vscode/
